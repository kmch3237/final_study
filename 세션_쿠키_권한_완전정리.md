# 세션 / 쿠키 / 권한 체크 완전 정리
작성일: 2026-05-31  
기준: 수업 필기 + finalProject 실제 코드

---

## 1. HTTP는 왜 세션/쿠키가 필요한가 — Stateless 문제

HTTP는 기본적으로 **Stateless(무상태)** 프로토콜이다.

```
무상태란?
서버는 요청 하나를 처리하고 나면
"그 사람이 누구였는지" 바로 잊어버린다.
```

```
[문제 상황]
① 로그인 요청  →  서버: "아 강명철이구나, 로그인 처리!"
② 카페 등록 요청 →  서버: "...이 사람 누구지? 모르겠는데?"
```

요청과 요청 사이에 연결이 없기 때문에  
**"이 사람이 로그인된 사람이다"** 라는 상태를 유지할 방법이 필요하다.  
→ 이것을 해결하는 방법이 **쿠키**와 **세션**이다.

---

## 2. 쿠키 (Cookie)

### 개념
- 서버가 브라우저에게 건네주는 **작은 메모지**
- 브라우저(클라이언트)의 하드디스크에 텍스트 파일로 저장됨
- 이후 요청마다 브라우저가 서버에 이 메모지를 자동으로 보여줌

### 동작 흐름
```
① 브라우저 → 서버 : "로그인 요청"
② 서버 → 브라우저 : "응답 + 쿠키(name=강명철) 전달"
③ 브라우저가 쿠키를 하드디스크에 저장
④ 브라우저 → 서버 : "카페 등록 요청" + 쿠키(name=강명철) 자동 첨부
⑤ 서버 : "아 강명철이구나!" → 처리
```

### 쿠키의 단점
- **클라이언트에 저장** → 사용자가 직접 수정/변조 가능 → 보안 취약
- 예: `userId=1` 저장했는데 사용자가 `userId=2` 로 바꿔버리면?
- 그래서 **로그인 같은 중요한 정보는 쿠키에 직접 저장하지 않는다**

### Java에서 쿠키 사용 (참고용)
```java
// 쿠키 생성 및 저장
Cookie c = new Cookie("name", "강명철");
c.setMaxAge(60 * 60);  // 1시간 유지 (초 단위)
response.addCookie(c); // 브라우저에 전달

// 쿠키 읽기
Cookie[] cookies = request.getCookies();

// 쿠키 삭제 (유효시간 0으로 설정)
c.setMaxAge(0);
response.addCookie(c);
```

### setMaxAge 값 의미
| 값 | 결과 |
|----|------|
| 양수 (예: 3600) | 해당 초만큼 유지 (브라우저 꺼도 살아있음) |
| 음수 (기본값) | 브라우저 종료 시 삭제 |
| 0 | 즉시 삭제 |

---

## 3. 세션 (Session)

### 개념
- **서버**에 사용자 정보를 저장하는 방식
- 서버가 각 사용자에게 고유한 **세션 ID**를 발급
- 세션 ID만 쿠키(`JSESSIONID`)로 브라우저에 저장
- 실제 데이터는 서버에 있음 → 보안성 높음

### 동작 흐름
```
① 브라우저 → 서버 : "로그인 요청 (아이디/비번)"
② 서버 : DB 확인 → 맞으면 세션 생성
         서버 메모리에 저장:
         세션ID "abc123" → { userId: 1, role: "OWNER" }
③ 서버 → 브라우저 : "응답 + JSESSIONID=abc123 쿠키 전달"
④ 브라우저가 JSESSIONID=abc123 쿠키 저장

⑤ 브라우저 → 서버 : "카페 등록 요청" + JSESSIONID=abc123 자동 첨부
⑥ 서버 : JSESSIONID=abc123으로 세션 조회
         → { userId: 1, role: "OWNER" } 발견
         → "아 userId=1인 OWNER구나!" → 처리
```

### 핵심 포인트
```
쿠키: 실제 데이터를 클라이언트에 저장  → 보안 취약
세션: 실제 데이터를 서버에 저장
      브라우저엔 세션ID(열쇠)만 쿠키로 저장 → 보안 안전
```

비유:
```
쿠키 = 지갑에 현금 직접 넣기 (잃어버리면 끝)
세션 = 은행 계좌 (통장은 은행에, 나는 카드(세션ID)만 들고 다님)
```

---

## 4. Spring Boot에서 세션 사용법

### 저장 — setAttribute
```java
// 로그인 처리 시 세션에 저장
@PostMapping("/login")
public String login(@RequestParam String loginId,
                    @RequestParam String password,
                    HttpSession session) {

    Member member = service.login(loginId, password);

    if (member != null) {
        session.setAttribute("loginMember", member); // Member 객체 통째로
        session.setAttribute("userId", member.getUserId()); // userId 따로
        session.setAttribute("role", member.getRole());     // 권한 따로
        return "redirect:/member/main";
    }
    return "redirect:/member/login";
}
```

### 꺼내기 — getAttribute
```java
// 다른 컨트롤러에서 세션 값 꺼내기
@PostMapping("/enroll")
public String enroll(Cafe cafe, HttpSession session) {

    Long userId = (Long) session.getAttribute("userId");
    // (Long) 캐스팅 필요 — getAttribute는 Object 타입으로 반환하기 때문

    cafe.setUserId(userId);
    cafeService.insertCafe(cafe);
    return "redirect:/cafe/enroll";
}
```

### 삭제 — invalidate / removeAttribute
```java
// 로그아웃: 세션 전체 삭제
@GetMapping("/logout")
public String logout(HttpSession session) {
    session.invalidate(); // 세션에 저장된 모든 값 삭제
    return "redirect:/member/login";
}

// 특정 값만 삭제
session.removeAttribute("userId");
```

### JSP에서 세션 값 사용
```jsp
<!-- JSTL 없이 -->
<%
    Member member = (Member) session.getAttribute("loginMember");
    String nickName = member.getNickName();
%>

<!-- EL 표현식으로 (더 간단) -->
${sessionScope.loginMember.nickName}
${sessionScope.userId}
```

---

## 5. 스코프(Scope) — 데이터의 유효 범위

| 스코프 | 유지 기간 | 공유 범위 | 사용 예 |
|--------|---------|---------|---------|
| page | 현재 JSP 1개 | 현재 페이지만 | 거의 안 씀 |
| request | 요청 1번 | 포워드로 연결된 페이지 | 컨트롤러 → JSP 데이터 전달 |
| session | 브라우저 종료까지 | 같은 브라우저의 모든 페이지 | 로그인 정보 |
| application | 서버 종료까지 | 모든 사용자 | 공통 설정값 |

```
로그인 정보 → session 스코프 (내가 로그인한 동안 유지)
게시글 목록 → request 스코프 (그 요청에서만 필요)
```

---

## 6. 파이널 프로젝트 권한 구조

### 권한(Role) 흐름
```
회원가입
    ↓
로그인 → 일반 사용자 (USER)
    ↓
카페 등록 신청 → 관리자 승인
    ↓
사장님 (OWNER) 권한 부여
    ↓  ↓
테마 등록   출석 체크   매니저 임명
가능        가능        가능
```

### DB에 권한 컬럼 필요
USER_ACCOUNT 테이블에 ROLE 컬럼이 있어야 한다.
```sql
-- USER_ACCOUNT 테이블
USER_ID   NUMBER
LOGIN_ID  VARCHAR2(50)
PASSWORD  VARCHAR2(100)
NICKNAME  VARCHAR2(50)
ROLE      VARCHAR2(20)   -- 'USER', 'OWNER', 'MANAGER', 'ADMIN'
```

### Member.java에 role 필드 추가
```java
@Getter
@Setter
@NoArgsConstructor
public class Member {
    private Long userId;
    private String loginId, nickName, name, email, phone, gender, birthDate, password;
    private String role;  // USER / OWNER / MANAGER / ADMIN
}
```

### 로그인 시 세션에 role 저장
```java
session.setAttribute("loginMember", member);
session.setAttribute("userId", member.getUserId());
session.setAttribute("role", member.getRole());  // "USER" or "OWNER"
```

---

## 7. 권한 체크 방법 2가지

### 방법 ① 컨트롤러에서 직접 체크

```java
@PostMapping("/enroll")
public String enroll(Cafe cafe, HttpSession session) {

    // 로그인 여부 확인
    Member loginMember = (Member) session.getAttribute("loginMember");
    if (loginMember == null) {
        return "redirect:/member/login"; // 비로그인 → 로그인 페이지
    }

    // 권한 확인
    String role = (String) session.getAttribute("role");
    if (!"OWNER".equals(role)) {
        return "redirect:/member/main"; // 사장님 아니면 차단
    }

    // 통과
    cafeService.insertCafe(cafe);
    return "redirect:/cafe/enroll";
}
```

- 장점: 간단하고 직관적
- 단점: 컨트롤러마다 같은 코드 반복해야 함
- `/owner/**` 경로가 여러 개면 모든 컨트롤러에 중복 코드 발생

---

### 방법 ② 인터셉터 (Interceptor) — 권장 방법

인터셉터는 컨트롤러 실행 **전에** 가로채서 권한을 먼저 확인하는 역할이다.

```
브라우저 요청
    ↓
[인터셉터] ← 여기서 먼저 체크
    ↓ 통과
컨트롤러
    ↓
서비스 → DB
```

#### Step 1: 인터셉터 클래스 생성
```java
// src/main/java/com/doit/app/interceptor/OwnerInterceptor.java

@Component
public class OwnerInterceptor implements HandlerInterceptor {

    @Override
    public boolean preHandle(HttpServletRequest request,
                             HttpServletResponse response,
                             Object handler) throws Exception {

        HttpSession session = request.getSession();
        Member loginMember = (Member) session.getAttribute("loginMember");

        // 로그인 안 됨
        if (loginMember == null) {
            response.sendRedirect("/member/login");
            return false; // 컨트롤러 실행 중단
        }

        // 권한 없음
        if (!"OWNER".equals(loginMember.getRole())) {
            response.sendRedirect("/member/main");
            return false; // 컨트롤러 실행 중단
        }

        return true; // 통과 → 컨트롤러 실행
    }
}
```

#### Step 2: 인터셉터 등록 (어떤 URL에 적용할지 설정)
```java
// src/main/java/com/doit/app/config/WebConfig.java

@Configuration
public class WebConfig implements WebMvcConfigurer {

    @Autowired
    private OwnerInterceptor ownerInterceptor;

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        registry.addInterceptor(ownerInterceptor)
                .addPathPatterns("/owner/**"); // /owner/로 시작하는 모든 URL에 적용
    }
}
```

#### 적용 결과
```
/owner/theme/regist  →  인터셉터 체크 → OWNER 아니면 차단
/owner/manager/appoint → 인터셉터 체크 → OWNER 아니면 차단
/cafe/enroll         →  인터셉터 없음 → 누구나 접근 가능
/member/login        →  인터셉터 없음 → 누구나 접근 가능
```

---

## 8. 파이널 프로젝트 전체 세션 흐름

### 회원가입 → 로그인 → 권한 획득 → 기능 사용

```
[1단계] 회원가입
  POST /member/enroll
  → DB INSERT (USER_ACCOUNT, USER_INFO)
  → redirect:/member/login

[2단계] 로그인
  POST /member/login
  → DB SELECT (loginId + password 일치 확인)
  → 일치하면:
     session.setAttribute("loginMember", member)
     session.setAttribute("userId", member.getUserId())
     session.setAttribute("role", member.getRole())  // "USER"
  → redirect:/member/main

[3단계] 카페 등록 (일반 사용자도 가능)
  GET  /cafe/enroll  → 폼 보여주기
  POST /cafe/enroll
  → session에서 userId 꺼냄
  → cafe.setUserId(userId)
  → DB INSERT
  → (관리자가 승인하면 role을 "OWNER"로 변경)

[4단계] 사장님(OWNER)으로 승격 후
  session의 role이 "OWNER"로 업데이트 필요
  → 재로그인 하거나 session.setAttribute("role", "OWNER") 갱신

[5단계] 테마 등록 (OWNER만 가능)
  GET  /owner/theme/regist  → 인터셉터 체크 → OWNER 통과
  POST /owner/theme/regist
  → session에서 userId 꺼냄
  → userId로 cafeId 조회
  → theme.setCafeId(cafeId)
  → DB INSERT

[6단계] 로그아웃
  GET /member/logout
  → session.invalidate()
  → redirect:/member/login
```

---

## 9. 로그인 체크 vs 권한 체크 vs 인터셉터 비교

| | 직접 체크 | 인터셉터 |
|--|---------|---------|
| 위치 | 컨트롤러 메서드 안 | 별도 클래스 |
| 코드 중복 | 컨트롤러마다 반복 | 한 곳에서 처리 |
| 유지보수 | 어려움 | 쉬움 |
| 적합한 경우 | URL 1~2개 | URL 여러 개 (/owner/**) |

---

## 10. 자주 하는 실수

### 실수 ① session.getAttribute 캐스팅 빠뜨리기
```java
// 틀림 — 컴파일 오류
Long userId = session.getAttribute("userId");

// 맞음 — (Long) 캐스팅 필요
Long userId = (Long) session.getAttribute("userId");
// getAttribute는 Object 타입으로 반환하기 때문
```

### 실수 ② 세션 null 체크 안 하기
```java
// 위험 — 비로그인 상태면 NullPointerException
Long userId = (Long) session.getAttribute("userId");
cafe.setUserId(userId); // userId가 null이면 오류!

// 안전 — null 체크 먼저
Long userId = (Long) session.getAttribute("userId");
if (userId == null) {
    return "redirect:/member/login";
}
```

### 실수 ③ role 비교 시 == 사용
```java
// 틀림 — String 비교는 == 안됨
if (role == "OWNER") { }

// 맞음 — .equals() 사용
if ("OWNER".equals(role)) { }
// "OWNER".equals(role) 순서로 쓰면 role이 null이어도 NullPointerException 안 남
```

---

> **한 줄 요약**
> HTTP는 요청마다 기억을 잃는다 → 세션으로 서버에 로그인 상태 저장 → 세션ID만 쿠키로 브라우저에 보관 → 요청마다 세션ID로 누군지 확인 → 인터셉터로 /owner/** 권한 체크 자동화
