# Day 12.5 — 이미지 경로 마이그레이션 + 출석체크 흐름 리팩터링 (2026-06-11)

day12(6/10)와 day13(6/12) 사이. **하나의 큰 커밋(`f083307`)에 두 작업 묶음.**
① 테마 이미지 저장 위치를 `webapp/uploads/theme` → `static/dist/images`로 대이동, `WebConfig.java` 삭제.
② 출석체크 컨트롤러를 슬림하게 만들고 자동처리/수동처리 통합 패턴 도입.

---

## 1. 이미지 경로 마이그레이션 — 학원 패턴 → Spring Boot 자동 매핑

### 1-A. 왜 옮겼는가

**기존(Day 10):**
```java
this.uploadPath = req.getSession().getServletContext().getRealPath("/uploads/theme");
```
- 위치: `src/main/webapp/uploads/theme/`
- 학원 sb02 노티스 패턴 그대로
- 정적 리소스로 노출하려면 별도 `WebConfig.java`에서 ResourceHandler 등록 필요:
  ```java
  registry.addResourceHandler("/uploads/**")
          .addResourceLocations("file:" + servletContext.getRealPath("/uploads/"));
  ```

**변경(Day 12.5):**
```java
this.uploadPath = new File("src/main/resources/static/dist/images").getAbsolutePath();
```
- 위치: `src/main/resources/static/dist/images/`
- Spring Boot의 **기본 정적 리소스 매핑**이 `/dist/images/...` URL 알아서 처리
- `WebConfig.java` **삭제** (필요 없어짐)
- 다른 정적 리소스(`/dist/css`, `/dist/js`, `/dist/images/logo.png` 등)와 같은 디렉토리 → 일관성 ↑

### 1-B. 마이그레이션 작업 순서

1. **이미지 파일 이동** = git mv (R100 rename)
   ```
   webapp/uploads/theme/17808304…jfif → resources/static/dist/images/17808304…jfif
   ```
   총 8개 파일 이동
2. **ThemeServiceImpl.init()** 의 `uploadPath` 한 줄 변경
3. **JSP 2곳 URL 수정** = `themeinfo.jsp`, `themelist.jsp`
   ```jsp
   <!-- Before -->
   <img src="${path}/uploads/theme/${dto.imagePath}">
   <!-- After -->
   <img src="${path}/dist/images/${dto.imagePath}">
   ```
4. **WebConfig.java 삭제** = 더 이상 custom ResourceHandler 필요 없음

### 1-C. 트레이드오프 (면접 톤)

| 관점 | 학원 패턴 (webapp/uploads) | Spring Boot 정적 (static/dist) |
|---|---|---|
| 코드량 | WebConfig.java 필요 | 설정 0줄 |
| 패키지 일관성 | 정적 리소스가 두 군데(webapp/css + uploads) | 한 군데(`static/`) |
| WAR 배포 | OK | ⚠️ `resources/`는 클래스패스. JAR/WAR 빌드되면 read-only 가능성 |
| dev/시연 편의 | OK | OK (STS hot reload 시 즉시 반영) |

**시연·학원 평가용으론 OK.** 만약 실무로 가면 외부 디렉토리(`/var/app/uploads`) + WebConfig로 다시 변경 권장. 면접 때 이걸 알고 있다고 말할 수 있으면 점수 +.

### 1-D. 함정 (혼자 다시 짤 때)

| 함정 | 증상 | 해결 |
|---|---|---|
| WebConfig.java 삭제 안 하고 새 폴더로 옮김 | `/uploads/**` 매핑이 죽은 폴더 가리킴 → 404 | WebConfig.java 통째 삭제 |
| `src/main/resources/static/` 폴더 자동 재컴파일 안 됨 | 새 업로드 파일이 안 보임 | STS에서 Refresh + Clean. Spring DevTools restart 의존 |
| `webapp/uploads/` 그대로 둠 | 옛 이미지가 두 경로에 남음 | git mv로 이동, webapp/uploads 폴더 통째 삭제 |
| 절대경로(`getAbsolutePath()`) 작업 디렉토리 의존 | 다른 PC에서 실행하면 경로 다름 | 작업 디렉토리 = 프로젝트 루트 보장 필요. 또는 `ClassPathResource` 사용 |

---

## 2. 출석체크 흐름 리팩터링 — Service에 책임 이전

### 2-A. 핵심 동기

Day 10 출석체크 컨트롤러의 `attendance()` 메서드가 **70줄짜리 god method**가 되어 있었음:
- DB 조회 (사장/매니저 분기)
- 세션의 `attendDraft` 가져오기
- 예약별 draft 개수 ↔ 파티원 총원 비교
- `doneList` / `partialList` 분류
- model.addAttribute 여러 번

→ 컨트롤러는 **요청 받기 + 서비스 호출 + 뷰 반환**만 해야 함. 학원 룰.

### 2-B. 변경 1: done/partial 분류 로직 → Service로 이동

**Before** (컨트롤러 안에 31줄):
```java
// 완성 : 모든 파티원 체크 끝
// 진행중 : 일부만 체크
List<Long> doneList = new ArrayList<>();
List<Long> partialList = new ArrayList<>();

@SuppressWarnings("unchecked")
List<AttendItemDTO> draftList = (List<AttendItemDTO>) session.getAttribute("attendDraft");
if (draftList != null) {

    // 중복 없는 reservationId 가져오기
    List<Long> resIds = new ArrayList<>();
    for (AttendItemDTO d : draftList) {
        if (!resIds.contains(d.getReservationId())) {
            resIds.add(d.getReservationId());
        }
    }

    // 예약별로 draft 개수와 TOTAL_MEMBER 비교
    for (Long rid : resIds) {
        int draftCount = 0;
        for (AttendItemDTO d : draftList) {
            if (rid.equals(d.getReservationId())) draftCount++;
        }
        int total = 0;
        for (AttendanceListDTO a : attendList) {
            if (rid.equals(a.getReservationId())) {
                total = a.getTotalMember();
                break;
            }
        }
        if (draftCount >= total) {
            doneList.add(rid);
        } else {
            partialList.add(rid);
        }
    }
}
model.addAttribute("doneList", doneList);
model.addAttribute("partialList", partialList);
```

**After** (컨트롤러 3줄):
```java
Map<String, List<Long>> status = attendanceService.checkStatus(session, attendList);
model.addAttribute("doneList", status.get("done"));
model.addAttribute("partialList", status.get("partial"));
```

**Service의 새 메서드** (`AttendanceService`):
```java
public Map<String, List<Long>> checkStatus(HttpSession session, List<AttendanceListDTO> attendList);
```

**왜 Map 반환?** 두 List(done/partial)를 한 번에 반환해야 해서.
- 학생 본인 룰 "Map 안 배워서 다른 방법" 있었지만, 이건 결과 묶음용 단일 사용 → 면접에선 "관련 결과 묶기 위해 Map 사용" 한 줄 답변 가능.

### 2-C. 변경 2: role 분기 일원화

**Before** (컨트롤러에서 직접 분기):
```java
List<AttendanceListDTO> attendList = null;
if ("OWNER".equals(role)) {
    attendList = attendanceService.selectListByOwnerUserId(loginUser.getUserId());
} else {
    attendList = attendanceService.selectListByManagerUserId(loginUser.getUserId());
}
```

**After** (Service 안에서 분기):
```java
List<AttendanceListDTO> attendList = attendanceService.selectAttendListByRole(loginUser.getUserId(), role);
```

**Service 새 메서드:**
```java
@Override
public List<AttendanceListDTO> selectAttendListByRole(Long userId, String role) {
    if ("OWNER".equals(role)) {
        return selectListByOwnerUserId(userId);
    } else {
        return selectListByManagerUserId(userId);
    }
}
```

**왜 컨트롤러에서 빼냈는가?**
- 컨트롤러는 "어떤 데이터 가져올지" 선택하지 말 것
- Service는 **세션의 role 보고 데이터 응답 책임** → SRP 원칙에 가까움
- 나중에 매니저용 SQL이 더 복잡해져도 컨트롤러 코드 변경 0

### 2-D. 변경 3: 죽은 메서드 2개 제거 (학원 룰 청소)

```java
// 삭제한 것 (Auto-generated stub)
public void attendAll(List<AttendanceListDTO> list, Long staffUserId) throws Exception;
public boolean isCheckable(Long reservationId);
```

이전에 인터페이스만 박아두고 구현은 빈 메서드(`// TODO`)였음. 학원 룰 **"죽은 코드 = 지뢰"** — 본인이 정리.

### 2-E. 변경 4 (핵심!): 자동처리 ↔ 수동처리 통합 패턴

**스토리:** 우리 시스템은 두 가지 경로로 ATTENDANCE에 INSERT됨.
1. **자동** — 방탈출 종료 30분 후 스케줄러가 ATTENDANCE만 박음 (DETAIL은 안 박힐 수 있음)
2. **수동** — 사장이 직접 출석체크 페이지에서 처리

**문제:** 스케줄러가 먼저 ATTENDANCE 박아놓은 예약을 사장이 보면? Day 10 코드:
```java
// 스케줄러가 먼저 박은 경우 스킵
if (mapper.selectAttendanceExists(reservationId) > 0) continue;
mapper.insertAttendance(head);  // 신규 ATTENDANCE
// ATTENDANCE_DETAIL INSERT
```
→ 스케줄러 처리분은 사장이 손댈 수 없음. ATTENDANCE_DETAIL 박을 길이 없음 (ATTENDANCE_ID 모르니까).

**해결 (Day 12.5):**
```java
Long existingId = mapper.selectAttendanceIdByReservationId(reservationId);

if (existingId != null) {
    head.setAttendanceId(existingId);    // 자동처리 ATTENDANCE_ID 재사용
} else {
    head.setReservationId(reservationId);
    head.setUserId(staffUserId);
    mapper.insertAttendance(head);        // 신규 ATTENDANCE 행
}

// ATTENDANCE_DETAIL은 무조건 INSERT (자동/수동 둘 다 채워줌)
for (AttendItemDTO dto : drafts) {
    if (!reservationId.equals(dto.getReservationId())) continue;
    dto.setAttendanceId(head.getAttendanceId());
    mapper.insertAttendDetailByUser(dto);
    // ...
}
```

**핵심 패턴:** "있으면 재사용, 없으면 INSERT" — 회원가입의 `selectKey` 재사용 패턴과 동일한 정신.

**새 매퍼 메서드:**
```xml
<select id="selectAttendanceIdByReservationId" parameterType="Long" resultType="Long">
    SELECT ATTENDANCE_ID FROM ATTENDANCE WHERE RESERVATION_ID = #{reservationId}
</select>
```

→ 단순 SELECT. NULL이면 자동처리 안 됨, 값 있으면 자동처리됨.

### 2-F. 변경 5: SQL 인덴테이션 정리

`selectCrewByReservationId` (한 예약의 파티원 조회) — 들여쓰기·공백만 정리. 로직 동일.

---

## 3. 면접 톤 Q&A

### Q. 왜 이미지 경로를 마이그레이션 하셨나요?
> 학원 sb02 패턴은 `webapp/uploads`에 저장하고 `WebConfig.java`로 ResourceHandler를 등록하는 방식이었는데, 다른 정적 리소스(`dist/css`, `dist/js`)와 위치가 분산되어 일관성이 떨어졌습니다. Spring Boot의 기본 정적 리소스 매핑이 `static/` 하위를 자동으로 잡아주기 때문에 `static/dist/images`로 옮기고 WebConfig.java를 삭제했습니다. 단, `resources/`는 클래스패스라 WAR 배포 시 read-only가 될 수 있다는 한계를 알고 있고, 실무로 가면 외부 디렉토리로 다시 분리할 계획입니다.

### Q. 컨트롤러의 30줄 분류 로직을 왜 Service로 옮겼나요?
> 컨트롤러는 "요청 받기 + 서비스 호출 + 뷰 반환"이 책임이고, "처리완료/부분완료를 판단"하는 비즈니스 로직은 Service에 속합니다. 옮긴 후 컨트롤러는 3줄로 줄었고, 나중에 분류 로직 변경 시 Service만 손대면 됩니다. SRP 원칙을 따른 리팩터링입니다.

### Q. role 분기를 Service로 옮긴 이유는?
> 컨트롤러가 "사장이면 메서드 A, 매니저면 메서드 B"라는 분기를 가지면, 매퍼 메서드가 늘어날 때마다 컨트롤러가 같이 바뀝니다. Service에 `selectAttendListByRole(userId, role)` 하나로 묶으니 컨트롤러는 한 줄 호출만 알면 됩니다. **인터페이스가 안정되고 내부가 자유로워지는** 패턴입니다.

### Q. 자동처리와 수동처리를 어떻게 통합했나요?
> 처리 경로가 두 개라 ATTENDANCE 행이 이미 있는지 먼저 확인합니다. 있으면 그 ATTENDANCE_ID를 재사용해서 ATTENDANCE_DETAIL만 INSERT하고, 없으면 ATTENDANCE 신규 INSERT 후 똑같이 DETAIL 박습니다. "있으면 재사용, 없으면 INSERT" 패턴 — 회원가입에서 selectKey로 가져온 USER_ID를 USER_INFO INSERT에 재사용하는 것과 같은 정신입니다.

### Q. 만약 자동처리에서 노쇼인데 ATTENDANCE_DETAIL이 안 박혔다면?
> 사장이 출석체크 페이지에 들어오면 그 예약이 보이고, "노쇼" 선택 후 최종확인하면 ATTENDANCE_ID를 재사용해서 DETAIL이 박힙니다. 사장 시점에선 자동/수동 구분 없이 동일하게 처리됩니다.

### Q. `attendAll`, `isCheckable` 빈 메서드를 왜 그냥 두지 않고 지웠나요?
> "지뢰"라서요. 빌드는 통과하지만 누가 호출하면 `null` 반환이나 `BindingException`이 즉시 터집니다. 사용 안 하는 코드는 즉시 제거가 학원 룰 + 본인 룰입니다.

### Q. WebConfig.java를 지운다고 사이드 이펙트는 없나요?
> WebConfig.java가 했던 일은 `/uploads/**` URL 매핑뿐이었고, 이미지를 `static/dist/images`로 옮겼으니 `/dist/images/**`는 Spring Boot가 자동으로 처리합니다. JSP의 `<img src>` 두 곳도 같이 수정했고, git status로 다른 정적 자원 영향은 없는지 확인했습니다.

---

## 4. 시연 흐름 (Day 12.5 추가분)

- 사이드바 [테마 관리] → [테마 등록] → 이미지 업로드 → `static/dist/images/...` 폴더에 파일 + DB 컬럼 확인
- 테마 상세 페이지에서 이미지 노출 확인 (`/dist/images/...` URL)
- 시연 시작 전 출석체크 페이지 진입 → 자동처리 + 수동처리 시나리오 모두 같은 흐름으로 처리됨

---

## 5. 다음 (Day 13 = 6/12로 이어짐)

- 출석기록 페이지 신규 (`/owner/attendance/history`)
- 아이디찾기 LOGIN_ID null 버그 수정
- AttendanceMapper 데드 메서드 5개 제거
- UserService 메서드명 통일

---

## 6. 핵심 패턴 카드 (Day 12.5 추가)

### 카드 6: 컨트롤러 슬림화 룰
```
컨트롤러 = 요청 받기 + 서비스 호출 + 뷰 반환 (얇게)
서비스    = 비즈니스 로직 + DB 매퍼 호출 (모든 분기·계산)
매퍼      = SQL만
```
**컨트롤러에 30줄 짜리 for/if 분류 코드가 있으면 Service로 옮길 시그널.**

### 카드 7: "있으면 재사용, 없으면 INSERT" 패턴
```java
T existing = mapper.selectByKey(key);
if (existing != null) {
    dto.setId(existing.getId());   // 재사용
} else {
    mapper.insert(dto);            // 신규
}
// 연관 INSERT는 무조건 (재사용/신규 둘 다 같은 ID)
mapper.insertRelated(dto);
```
회원가입(USER_ACCOUNT→USER_INFO), 출석체크(ATTENDANCE→ATTENDANCE_DETAIL) 등 다양한 곳에서 반복.

### 카드 8: role 분기는 Service 안에서
```java
// 컨트롤러
service.doByRole(args, role);

// Service
if ("OWNER".equals(role)) { ... } else { ... }
```
컨트롤러가 role 모름. 인터페이스 안정 + 내부 자유.

---

## 7. 빠른 참고 — 이날 영향받은 파일 8개

| 파일 | 변경 종류 |
|---|---|
| `OwnerAttendanceController.java` | 메서드 슬림화 (30+줄 → 3줄) |
| `AttendanceService.java` | 메서드 2개 추가, 죽은 메서드 2개 삭제 |
| `AttendanceServiceImpl.java` | `checkStatus`, `selectAttendListByRole` 구현 + finalize 통합 |
| `AttendanceMapper.java` | `selectAttendanceIdByReservationId` 1개 추가 |
| `attendanceMapper.xml` | SELECT 1개 추가, `selectCrewByReservationId` 들여쓰기 정리 |
| `ThemeServiceImpl.java` | `uploadPath` 경로 1줄 변경 |
| `WebConfig.java` | **삭제** |
| `themeinfo.jsp`, `themelist.jsp` | 이미지 URL prefix 변경 |
| `noExit_kmc.sql` | 학생 본인 PL/SQL 추가/수정 |
| 이미지 파일 8개 | webapp/uploads/theme → static/dist/images 이동 (R100) |
